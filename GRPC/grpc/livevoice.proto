syntax = "proto3";
package livevoice;

service VoiceService {
  rpc TranslateAudio (AudioRequest) returns (AudioResponse);
}

message AudioRequest {
  bytes audio_chunk = 1;
}

message AudioResponse {
  string translated_text = 1;
  bytes tts_audio = 2;
}